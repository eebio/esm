## Pure menu navigation
## Enter every menu and click on every button and make sure no errors are thrown

# Main menu -> Transforms -> Test Transform.
EXPECT_WAIT:Main Menu (press [q] to quit):
ENTER
EXPECT_WAIT:Main Menu / Transforms (press [q] to quit):

# Test transform
ENTER
EXPECT:Test Transform> 
TEXT:1 + 1
ENTER
EXPECT:Transform evaluated successfully.
EXPECT:Test Transform> 
ENTER
EXPECT_WAIT:Main Menu / Transforms (press [q] to quit):

# Add transform
DOWN
ENTER
EXPECT:New Transform Name>
ENTER

# Remove transform
EXPECT_WAIT:Main Menu / Transforms (press [q] to quit):
DOWN
DOWN
ENTER
EXPECT_WAIT:Select a transform to remove
DOWN
EXPECT:> od_sub
DOWN
ENTER

# Edit transform
EXPECT_WAIT:Main Menu / Transforms (press [q] to quit):
DOWN
DOWN
DOWN
ENTER
EXPECT_WAIT:Select a transform to edit
DOWN
EXPECT:> od_sub
DOWN
ENTER

# Back from Transforms
EXPECT_WAIT:Main Menu / Transforms (press [q] to quit):
DOWN
DOWN
DOWN
DOWN
EXPECT:> Back
ENTER

# Visit Groups and exercise every group operation.
EXPECT_WAIT:Main Menu (press [q] to quit):
DOWN
ENTER

# Add Group, then cancel the name prompt.
EXPECT_WAIT:Main Menu / Groups (press [q] to quit):
ENTER
EXPECT:New Group Name>
ENTER

# Remove Group, then choose Back from the group selection menu.
EXPECT_WAIT:Main Menu / Groups (press [q] to quit):
DOWN
ENTER
EXPECT_WAIT:Select a group to remove
DOWN
DOWN
DOWN
ENTER

# Edit Group, then choose Back from the group selection menu.
EXPECT_WAIT:Main Menu / Groups (press [q] to quit):
DOWN
DOWN
ENTER
EXPECT_WAIT:Select a group to edit
DOWN
DOWN
DOWN
ENTER

# Enter Group Metadata and exercise every metadata operation.
EXPECT_WAIT:Main Menu / Groups (press [q] to quit):
DOWN
DOWN
DOWN
ENTER

# Add Group Metadata, then cancel the field-name prompt.
EXPECT_WAIT:Main Menu / Groups / Group Metadata (press [q] to quit):
ENTER
EXPECT:New Group Metadata Field Name>
ENTER

# Remove Group Metadata, then choose Back from the field selection menu.
EXPECT_WAIT:Main Menu / Groups / Group Metadata (press [q] to quit):
DOWN
ENTER
EXPECT_WAIT:Select a group metadata field to remove
ENTER

# Edit Group Metadata, then choose Back from the field selection menu.
EXPECT_WAIT:Main Menu / Groups / Group Metadata (press [q] to quit):
DOWN
DOWN
ENTER
EXPECT_WAIT:Select a group metadata field to edit
ENTER

# Back to Groups, then back to the main menu.
EXPECT_WAIT:Main Menu / Groups / Group Metadata (press [q] to quit):
DOWN
DOWN
DOWN
ENTER
EXPECT_WAIT:Main Menu / Groups (press [q] to quit):
DOWN
DOWN
DOWN
DOWN
ENTER

# Visit Views and exercise every view operation.
EXPECT_WAIT:Main Menu (press [q] to quit):
DOWN
DOWN
ENTER

# Produce the first available view.
EXPECT_WAIT:Main Menu / Views (press [q] to quit):
ENTER
EXPECT_WAIT:Select a view to produce
ENTER

# Add View, then cancel the name prompt.
EXPECT_WAIT:Main Menu / Views (press [q] to quit):
DOWN
ENTER
EXPECT:New View Name>
ENTER

# Remove View, then choose Back from the view selection menu.
EXPECT_WAIT:Main Menu / Views (press [q] to quit):
DOWN
DOWN
ENTER
EXPECT_WAIT:Select a view to remove
DOWN
DOWN
DOWN
DOWN
DOWN
DOWN
DOWN
ENTER

# Edit View, then choose Back from the view selection menu.
EXPECT_WAIT:Main Menu / Views (press [q] to quit):
DOWN
DOWN
DOWN
ENTER
EXPECT_WAIT:Select a view to edit
DOWN
DOWN
DOWN
DOWN
DOWN
DOWN
DOWN
ENTER

# Back to the main menu.
EXPECT_WAIT:Main Menu / Views (press [q] to quit):
DOWN
DOWN
DOWN
DOWN
ENTER

# Visit Save Changes, cancel the filename prompt, then quit.
EXPECT_WAIT:Main Menu (press [q] to quit):
DOWN
DOWN
DOWN
ENTER
EXPECT:Filename> 
ENTER
EXPECT_WAIT:Main Menu (press [q] to quit):
Q
EXPECT:No changes made.